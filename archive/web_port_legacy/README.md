# Legacy JavaScript viewer (archived)

This directory preserves the obsolete scene-JSON/JavaScript viewer as a
reverse-engineering reference. It is not the active browser port and should not
receive runtime fixes.

The supported browser implementation is `../../web_port_v2/`, backed by the
shared C++ runtime compiled to WebAssembly.
